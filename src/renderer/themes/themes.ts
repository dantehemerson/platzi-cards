type Theme = {
  textColor: string;
  textHighColor: string;
  background: string;
};

export const themes: Record<string, Theme> = {
  platzi: {
    textColor: '#263b47',
    textHighColor: '#7fc719',
    background: '#ffffff',
  },
};
